# Use Vitest for testing

The project uses Vitest (not Jest) for unit and component tests. Next.js 15+ with the App Router and React 19 has better compatibility with Vitest's ESM-first approach. Testing Library is used for component tests. Test files live alongside source files using the `*.test.ts(x)` convention.
